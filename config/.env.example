# Copy to `config/.env`. Real keys only in `config/.env` (gitignored).

OPENAI_API_KEY=
# Optional (default in agent/tools/ping_openai.py is gpt-4o-mini)
# OPENAI_MODEL=gpt-4o

# ANTHROPIC_API_KEY=
# AWS_ACCESS_KEY_ID=
# AWS_SECRET_ACCESS_KEY=
# AWS_REGION=us-east-1
